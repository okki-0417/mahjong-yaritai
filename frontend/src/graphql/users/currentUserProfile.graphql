query CurrentUserProfile {
  currentSession {
    isLoggedIn
    user {
      id
      name
      email
      profileText
      avatarUrl
      isFollowing
      followingCount
      followersCount
      createdAt
      updatedAt
    }
  }
}
